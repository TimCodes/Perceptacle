{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "perceptacle.fullname" . }}
  labels:
    {{- include "perceptacle.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                {{- if eq .service "server" }}
                name: {{ include "perceptacle.fullname" $ }}-server
                {{- else if eq .service "agents" }}
                name: {{ include "perceptacle.fullname" $ }}-agents
                {{- else if eq .service "client" }}
                name: {{ include "perceptacle.fullname" $ }}-client
                {{- else }}
                name: {{ .service }}
                {{- end }}
                port:
                  number: {{ .port }}
          {{- end }}
    {{- end }}
{{- end }}
